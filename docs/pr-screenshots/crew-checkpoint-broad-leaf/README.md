# Room5 broad fallen gate

Independent native desktop and portrait review accepts this fallen gate as a bounded model improvement. The broad framed face, displaced corner and torn edge replace the narrow rack-like rough. The protected station and wall-connected partition remain unchanged. The opposite northern stump is unfinished. Full Room5 and DM535120 remain open.

The reproducible asset is `src/render/CrewCheckpointBlockout.ts`. Only the fallen branch and its matching Room5 footprint changed. The new footprint is `[610,484,88,112]`. Existing production radius16/28 movement and shot checks pass. Both cross-room snapshots differ only at that obstacle, verified by parsed comparison.

## Evidence limits

Eleven of twelve requested static integrated-app views completed, five desktop at 1280x900 and six portrait at 390x844. Desktop approach timed out; the aggregate capture failed and remains failed. Available gate, crew-side and operator views support the bounded model verdict. Each captured before/after state is identical. Enemies are absent. These are staged legal poses with production HUD, unchanged shipping cameras, cached fonts and software WebGL, not live combat or physical-device tests.

Before images remain in `../crew-checkpoint-fallen-breach/`. All eleven current originals are in this directory. The phone north HUD visibility and prior release dependencies remain open. No merge or deployment.

## Verification

17 focused tests passed. Vitest passed 776 with one skipped. Two Node tests and TypeScript/Vite build passed. Initial test failures and the screenshot timeout remain in the local batch record. No full local release-verifier claim.

Independent art review inspected all eleven candidate originals and six baseline originals before the diff. Independent CPU review confirmed Room5-only scope. Its support-test caveat remains: AABB overlap alone does not prove rod-to-plate contact. Source construction and native pixels showed no demonstrated floating defect.
