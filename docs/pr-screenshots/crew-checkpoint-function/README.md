# Room5 controlled checkpoint

The wall-connected layout and protected station earned bounded independent visual acceptance. DM535120 is not fully fixed. The damaged gate still reads as a chipped jamb beside an ordinary opening. Next replace that cue with a recognizable displaced gate section, preserving the accepted partition and playable passage. No third tint or silhouette-only polish pass.

Native before/after desktop and portrait originals are below. `provenance.json` records their source, hashes, legal actor positions and shipping camera state. These are staged app stills with the actual Skip HUD, not live combat or release evidence. The northern phone HUD dependency remains open.

Production radius16/28 movement, open/blocked shots, no end bypass, grounded support and footprint checks pass. Final code passed774 Vitest tests with one skipped; the four-file affected-map subset passed48 tests. Build passed with the existing bundle-size warning. Two Node preview tests passed on the final source. Full release verification is not claimed.

Only Room5 map/model and focused tests changed. Other room snapshots differ only in the Room5 entry. The final source matches reviewed6e0143d. An enlarged-leaf attempt was rejected and reverted; its source and raw captures remain in local production history.
