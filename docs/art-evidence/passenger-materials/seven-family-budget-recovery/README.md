# Seven-family material diagnostic

Material gate not passed. This is a nonintegrated, inactive presentation fixture, not playable or deployed completion.

The fixture now loads all seven actual budget-package GLBs across 25 placements. It replaces reserved equipment only in a temporary Vite build, retaining production room layout, HUD, high-quality lighting and shipping framing. Full-room and closeup images are diagnostic projections. The before package uses original ancillary exports and the unchanged chamber/carrier wear exports.

The first capture failed on incompatible monitor geometry attributes. `../seven-family-budget/` preserves that failure. The recovery separates attribute-compatible geometry batches and refuses existing evidence destinations. Both browsers and the temporary preview/build completed cleanup.

## Verified

- 34 requested desktop/portrait frames, exact dimensions, unique states and file hashes.
- 232 captured source hashes match. Shipping `src/` and `public/` are unchanged in this slice.
- Parent `npm test` and `npm run build` exited zero. Vitest reports 519 passed and one optional skip; required Node/Python checks also completed. The fixture test is included in the default Vitest suite.
- [Independent review](independent-review.md) covers all images through contact sheets and native representatives. Parent additionally inspected native north-loop, distribution/monitor closeups and portrait monitor views.
- [Parent verification](parent-verification.json) links the completed review. Raw manifest review fields remain historical `pending` values, not the current verdict.

No local full browser verifier was run for this tooling-only slice. The real fixture capture completed in desktop and portrait. CI on this new commit is reported in PR59 rather than inferred from the previous head.

## Outcome and changed approach

These images expose unfinished ancillary surfaces. The service console remains pale with a near-white recessed face; cabinet panels and fittings lack convincingly distinct material response. Wear is not visible. Portrait crops and HUD coverage limit passenger cue claims. No new room-art gate passed.

After two consecutive runs without art-stage progress, the next batch changes from capture/budget work to actual ancillary material authoring. Start with the monitor enclosure, display and handled service edges under the unchanged palette ceiling, geometry, layout, camera and lighting. Require an independently identified material/maintained-wear outcome before counting progress. If this bounded recovery fails, preserve diagnostics and pause the worker under the production policy. Do not integrate or merge unfinished materials.

## Reproduce

Run `node scripts/passenger-material-evidence.mjs <fresh-explicit-output-directory>` from the repository root. Existing destinations are rejected, including empty directories. Do not overwrite historical evidence. This comparison is before/budget, not the historical neutral/material/wear matrix.
